In reflection, I tried to go method by method in writing tests because I thought it worked well
in the test driven development style creating methods from each test.
I felt like understood what each test was testing for and covered edge cases well.
I did keep much of the AI generated content but was very specific with prompts and
I used Copilot. I did not accept the attempt at removing the "public" modifier for BagTest
because that was preexisting in the source code template.

I liked the way that the tests were generated and how size() deals with null
values that are elements. I reflect on how datasets I have worked with
in the past have had null values and how they count them and are considered. It
was interesting to see this use in Java in comparison to how it looks in the Numpy or Pandas 
libraries, very similar. 